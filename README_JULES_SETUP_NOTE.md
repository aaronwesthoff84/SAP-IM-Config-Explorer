# Jules Setup Note

The full operating guide is in [`docs/JULES_AUTOMATION.md`](docs/JULES_AUTOMATION.md). After this setup pull request merges, apply the `jules` label only to Issues that satisfy the readiness contract in `AGENTS.md` and the Issue Forms.
